# blockchain.js
A blockchain running on JavaScript

## Build
Run:

```bash
./build.sh
```

## Serve
Run:

```bash
./serve.sh
```

## Run tests
Run:

```bash
./test.sh
```

## Enter the container
Run:

```bash
./ssh.sh
```

to start a container and run a bash inside it.
